name: CI

# Delegates to the shared PowerShell CI workflow in PowerShell-Common.
# Kept as a local file so the pull_request trigger works for PRs in this repo,
# and so release.yml can call it via workflow_call without an external reference.
on:
  pull_request:
    branches: [ main, master ]
  workflow_call:
  workflow_dispatch:

jobs:
  ci:
    uses: VitaliiAndreev/PowerShell-Common/.github/workflows/ci-powershell.yml@master
