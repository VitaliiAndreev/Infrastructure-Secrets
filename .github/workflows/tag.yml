name: Tag from manifest version

on:
  push:
    branches:
      - master
    paths:
      # Only fire when the manifest itself changes, not on every push.
      - 'Infrastructure.Secrets/Infrastructure.Secrets.psd1'
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/ci.yml

  test-integration:
    uses: ./.github/workflows/ci-docker.yml

  tag:
    needs: [ test, test-integration ]
    runs-on: windows-latest
    permissions:
      # Required to create and push the tag.
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          # Full history is required so `git tag -l` can see all existing
          # tags and avoid creating duplicates.
          fetch-depth: 0

      - uses: VitaliiAndreev/Infrastructure-Common/.github/actions/tag-from-manifest@master
        with:
          psd1: 'Infrastructure.Secrets\Infrastructure.Secrets.psd1'

  publish:
    needs: tag
    uses: VitaliiAndreev/Infrastructure-Common/.github/workflows/publish.yml@master
    with:
      module-path: Infrastructure.Secrets
    secrets:
      PSGALLERY_API_KEY: ${{ secrets.PSGALLERY_API_KEY }}
